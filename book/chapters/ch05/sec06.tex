% ============================================================================
% SEC06.TEX - Section 5.6: Camera Control
% ============================================================================

\section{Camera Control}

\begin{learningoutcomes}
\item Mengimplementasikan follow camera
\item Membuat third-person camera
\item Menggunakan Cinemachine untuk camera control
\end{learningoutcomes}

\subsection{Follow Camera}

\begin{lstlisting}[language={[Sharp]C}]
public class FollowCamera : MonoBehaviour
{
    public Transform target;
    public Vector3 offset = new Vector3(0, 5, -10);
    public float smoothSpeed = 0.125f;
    
    void LateUpdate()
    {
        Vector3 desiredPosition = target.position + offset;
        Vector3 smoothedPosition = Vector3.Lerp(transform.position, desiredPosition, smoothSpeed);
        transform.position = smoothedPosition;
        
        transform.LookAt(target);
    }
}
\end{lstlisting}

\subsection{Third-Person Camera}

\begin{lstlisting}[language={[Sharp]C}]
public class ThirdPersonCamera : MonoBehaviour
{
    public Transform target;
    public float distance = 5f;
    public float height = 2f;
    public float rotationSpeed = 2f;
    
    private float currentRotationX;
    private float currentRotationY;
    
    void LateUpdate()
    {
        // Mouse input for rotation
        currentRotationX += Input.GetAxis("Mouse X") * rotationSpeed;
        currentRotationY -= Input.GetAxis("Mouse Y") * rotationSpeed;
        currentRotationY = Mathf.Clamp(currentRotationY, -35, 60);
        
        // Calculate position
        Quaternion rotation = Quaternion.Euler(currentRotationY, currentRotationX, 0);
        Vector3 position = target.position - rotation * Vector3.forward * distance + Vector3.up * height;
        
        transform.position = position;
        transform.LookAt(target);
    }
}
\end{lstlisting}

\begin{catatan}
Gunakan LateUpdate() untuk camera movement agar mengikuti target setelah semua movement selesai. Cinemachine package menyediakan solusi camera yang lebih advanced.
\end{catatan}

\subsection{Ringkasan Section}

\begin{itemize}
\item Follow camera mengikuti target dengan offset
\item Third-person camera memberikan kontrol rotasi
\item LateUpdate() digunakan untuk camera movement
\item Cinemachine menyediakan solusi camera yang lebih advanced
\end{itemize}
